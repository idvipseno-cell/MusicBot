# 🎵 Seno Music Bot - بوت الموسيقى الاحترافي

<div align="center">

![Seno Music Bot](https://via.placeholder.com/800x200/6c5ce7/ffffff?text=Seno+Music+Bot)

### 🎧 بوت تشغيل الموسيقى الأكثر تطوراً في تيليجرام

**المطور:** سينو (Seno) - [@idseno](https://t.me/idseno)  
**القناة الرسمية:** [@senovip](https://t.me/senovip)

---

</div>

## ✨ المميزات

- 🎵 تشغيل الأغاني في مكالمات المجموعات والقنوات
- 📥 تنزيل الأغاني بجودة عالية
- 🤖 حساب مساعد للتشغيل
- 🎨 واجهة عربية بالكامل بدون شخطات
- 🖼️ صور احترافية للرسائل
- 💎 أزرار شفافة أنيقة
- 🔒 اشتراك إجباري بقناة المطور
- 💰 نظام تنصيب البوتات باشتراك شهري ($10)
- 🎯 تصميم عصري ومتطور

---

## 📋 المتطلبات

قبل البدء، تأكد من توفر:

1. **Python 3.9+** مثبت على السيرفر
2. **Git** لاستنساخ المشروع
3. **FFmpeg** لمعالجة الصوتيات
4. حساب تيليجرام

---

## 🔑 الحصول على API Keys

### 1️⃣ API ID & API Hash من Telegram

1. اذهب إلى: https://my.telegram.org
2. سجل دخول برقم هاتفك
3. اذهب إلى **API Development Tools**
4. أنشئ تطبيق جديد
5. احفظ `api_id` و `api_hash`

### 2️⃣ Bot Token من BotFather

1. افتح [@BotFather](https://t.me/BotFather) في تيليجرام
2. أرسل الأمر `/newbot`
3. اختر اسم للبوت ثم username (يجب أن ينتهي بـ bot)
4. احفظ الـ **Token**

### 3️⃣ إنشاء الحساب المساعد

1. أنشئ حساب تيليجرام جديد برقم آخر
2. احفظ رقم الهاتف
3. ستحتاجه للـ String Session

---

## 🚀 خطوات التثبيت

### الطريقة الأولى: التثبيت المحلي

```bash
# 1. استنساخ المشروع
git clone https://github.com/YOUR-USERNAME/seno-music-bot.git
cd seno-music-bot

# 2. تثبيت المكتبات المطلوبة
pip3 install -r requirements.txt

# 3. تثبيت FFmpeg
# Ubuntu/Debian:
sudo apt-get update
sudo apt-get install ffmpeg -y

# CentOS/RHEL:
sudo yum install ffmpeg -y

# macOS:
brew install ffmpeg
```

### الطريقة الثانية: Heroku

```bash
# 1. تثبيت Heroku CLI
curl https://cli-assets.heroku.com/install.sh | sh

# 2. تسجيل الدخول
heroku login

# 3. إنشاء التطبيق
heroku create seno-music-bot

# 4. إضافة Buildpacks
heroku buildpacks:add heroku/python
heroku buildpacks:add https://github.com/jonathanong/heroku-buildpack-ffmpeg-latest.git

# 5. رفع البوت
git push heroku main
```

### الطريقة الثالثة: VPS

```bash
# تثبيت على Ubuntu/Debian VPS
sudo apt-get update
sudo apt-get upgrade -y
sudo apt-get install python3-pip python3-dev ffmpeg git -y

git clone https://github.com/YOUR-USERNAME/seno-music-bot.git
cd seno-music-bot
pip3 install -r requirements.txt
```

---

## ⚙️ الإعدادات

### 1. ملف .env

أنشئ ملف `.env` في المجلد الرئيسي:

```env
# معلومات API من my.telegram.org
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890

# توكن البوت من @BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# String Session للحساب المساعد (سنحصل عليه بالخطوة التالية)
STRING_SESSION=

# معرف المطور (معرفك الرقمي)
OWNER_ID=123456789

# رابط قناة المطور للاشتراك الإجباري
CHANNEL_USERNAME=@senovip

# معرف قناة المطور (ID رقمي، اختياري)
CHANNEL_ID=-1001234567890

# اسم المطور
DEVELOPER_NAME=سينو

# رابط حساب المطور
DEVELOPER_USERNAME=@idseno

# سعر الاشتراك الشهري
SUBSCRIPTION_PRICE=10

# MongoDB (اختياري - للبيانات المتقدمة)
MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net/
```

### 2. الحصول على String Session

```bash
# شغل سكريبت الحصول على Session
python3 generate_session.py
```

اتبع التعليمات:
1. أدخل API ID
2. أدخل API Hash
3. أدخل رقم الهاتف للحساب المساعد
4. أدخل كود التحقق
5. أدخل كلمة المرور (إن وجدت)
6. انسخ الـ String Session والصقه في `.env`

---

## ▶️ تشغيل البوت

### التشغيل العادي

```bash
python3 main.py
```

### التشغيل في الخلفية (باستخدام Screen)

```bash
# تثبيت screen
sudo apt-get install screen -y

# إنشاء جلسة جديدة
screen -S seno-music

# تشغيل البوت
python3 main.py

# للخروج من الجلسة: اضغط Ctrl+A ثم D
# للعودة للجلسة: screen -r seno-music
```

### التشغيل باستخدام PM2

```bash
# تثبيت Node.js و PM2
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt-get install -y nodejs
sudo npm install pm2 -g

# تشغيل البوت
pm2 start main.py --name seno-music --interpreter python3

# أوامر مفيدة:
pm2 list          # عرض البوتات
pm2 stop seno-music    # إيقاف
pm2 restart seno-music # إعادة تشغيل
pm2 logs seno-music    # عرض اللوجات
pm2 startup        # التشغيل التلقائي عند إعادة التشغيل
pm2 save          # حفظ الإعدادات
```

---

## 📱 الأوامر المتاحة

### أوامر التشغيل
- `شغل` - تشغيل أغنية من يوتيوب/سبوتيفاي
- `ايقاف` - إيقاف التشغيل الحالي
- `تخطي` - تخطي للأغنية التالية
- `استمرار` - استكمال التشغيل بعد الإيقاف المؤقت
- `وقف` - إيقاف مؤقت
- `قائمة` - عرض قائمة التشغيل

### أوامر التنزيل
- `نزل` - تنزيل أغنية
- `بحث` - البحث عن أغنية

### أوامر الإدارة (للمطور فقط)
- `احصائيات` - عرض إحصائيات البوت
- `اذاعة` - إرسال رسالة لجميع المستخدمين
- `المستخدمين` - عرض عدد المستخدمين

---

## 🏗️ هيكل المشروع

```
seno-music-bot/
│
├── main.py                 # ملف التشغيل الرئيسي
├── config.py               # ملف الإعدادات
├── generate_session.py     # سكريبت توليد String Session
│
├── handlers/               # معالجات الأوامر
│   ├── __init__.py
│   ├── music.py           # أوامر الموسيقى
│   ├── download.py        # أوامر التنزيل
│   ├── admin.py           # أوامر الإدارة
│   └── subscription.py    # نظام الاشتراك
│
├── helpers/               # وظائف مساعدة
│   ├── __init__.py
│   ├── youtube.py        # التعامل مع يوتيوب
│   ├── decorators.py     # ديكوريتورز مخصصة
│   └── database.py       # قاعدة البيانات
│
├── assets/               # الملفات الثابتة
│   ├── images/          # الصور
│   │   ├── start.jpg
│   │   ├── playing.jpg
│   │   └── download.jpg
│   └── fonts/           # الخطوط العربية
│
├── requirements.txt      # المكتبات المطلوبة
├── Procfile             # لـ Heroku
├── runtime.txt          # إصدار Python
├── .env                 # المتغيرات (لا تشاركه!)
├── .gitignore          
└── README.md            # هذا الملف
```

---

## 🛠️ حل المشاكل الشائعة

### المشكلة: "No module named 'pyrogram'"
**الحل:**
```bash
pip3 install -r requirements.txt --upgrade
```

### المشكلة: "FFmpeg not found"
**الحل:**
```bash
sudo apt-get install ffmpeg -y
```

### المشكلة: "Invalid string session"
**الحل:**
1. احذف الـ String Session القديم
2. شغل `python3 generate_session.py` مرة أخرى
3. تأكد من استخدام رقم الحساب المساعد الصحيح

### المشكلة: البوت لا يرد
**الحل:**
1. تأكد من تشغيل البوت بنجاح
2. تحقق من صحة BOT_TOKEN
3. راجع لوجات الأخطاء

---

## 💎 نظام التنصيب والاشتراكات

البوت يوفر نظام تنصيب احترافي:

### للمطور (أنت):
- إدارة كاملة على جميع البوتات
- تحصيل اشتراكات شهرية ($10/شهر)
- لوحة تحكم شاملة

### للمشتركين:
- بوت كامل بحقوقهم
- دعم فني مستمر
- تحديثات دورية
- استضافة آمنة

---

## 📞 الدعم والتواصل

- **المطور:** سينو (Seno)
- **التيليجرام:** [@idseno](https://t.me/idseno)
- **القناة:** [@senovip](https://t.me/senovip)

---

## 📄 الترخيص

جميع الحقوق محفوظة © 2025 - Seno (@idseno)

هذا المشروع ملك خاص ولا يسمح باستخدامه أو توزيعه بدون إذن صريح من المطور.

---

## 🌟 شكر خاص

شكراً لاختيارك Seno Music Bot - البوت الأكثر تطوراً واحترافية! 🎵

---

<div align="center">

**صُنع بـ ❤️ بواسطة [سينو](https://t.me/idseno)**

</div>
