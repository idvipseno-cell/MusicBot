"""
Seno Music Bot - Configuration File
المطور: سينو (Seno) - @idseno
القناة: @senovip
"""

import os
from dotenv import load_dotenv

load_dotenv()

class Config:
    """إعدادات البوت الرئيسية"""
    
    # معلومات API من my.telegram.org
    API_ID = int(os.getenv("API_ID", "0"))
    API_HASH = os.getenv("API_HASH", "")
    
    # توكن البوت من @BotFather
    BOT_TOKEN = os.getenv("BOT_TOKEN", "")
    
    # String Session للحساب المساعد
    STRING_SESSION = os.getenv("STRING_SESSION", "")
    
    # معلومات المطور
    OWNER_ID = int(os.getenv("OWNER_ID", "0"))
    DEVELOPER_NAME = os.getenv("DEVELOPER_NAME", "سينو")
    DEVELOPER_USERNAME = os.getenv("DEVELOPER_USERNAME", "@idseno")
    
    # قناة المطور للاشتراك الإجباري
    CHANNEL_USERNAME = os.getenv("CHANNEL_USERNAME", "@senovip")
    CHANNEL_ID = int(os.getenv("CHANNEL_ID", "0")) if os.getenv("CHANNEL_ID") else None
    
    # نظام الاشتراكات
    SUBSCRIPTION_PRICE = os.getenv("SUBSCRIPTION_PRICE", "10")
    
    # MongoDB (اختياري)
    MONGO_URI = os.getenv("MONGO_URI", "")
    
    # إعدادات الصور
    START_IMG = "https://telegra.ph/file/your-start-image.jpg"
    PLAYING_IMG = "https://telegra.ph/file/your-playing-image.jpg"
    DOWNLOAD_IMG = "https://telegra.ph/file/your-download-image.jpg"
    
    # رسائل البوت
    START_TEXT = """
🎵 **أهلاً بك في بوت سينو للموسيقى!**

البوت الأكثر تطوراً لتشغيل الأغاني في مكالمات تيليجرام 🎧

**المميزات:**
━━━━━━━━━━━━━━
✅ تشغيل من يوتيوب وسبوتيفاي
✅ جودة صوت عالية
✅ تنزيل الأغاني
✅ قوائم تشغيل ذكية
✅ واجهة عربية سهلة

**الأوامر الرئيسية:**
━━━━━━━━━━━━━━
🎵 شغل - لتشغيل أغنية
📥 نزل - لتنزيل أغنية
⏸ ايقاف - لإيقاف التشغيل
⏭ تخطي - للأغنية التالية

━━━━━━━━━━━━━━
👨‍💻 **المطور:** {developer}
📢 **القناة:** {channel}
"""
    
    HELP_TEXT = """
📚 **قائمة الأوامر الكاملة**

**🎵 أوامر التشغيل:**
━━━━━━━━━━━━━━
• `شغل` [اسم الأغنية أو رابط]
  لتشغيل أغنية في المكالمة

• `ايقاف`
  لإيقاف التشغيل تماماً

• `وقف`
  لإيقاف التشغيل مؤقتاً

• `استمرار`
  لاستكمال التشغيل

• `تخطي`
  للانتقال للأغنية التالية

• `قائمة`
  لعرض قائمة التشغيل الحالية

**📥 أوامر التنزيل:**
━━━━━━━━━━━━━━
• `نزل` [اسم الأغنية أو رابط]
  لتنزيل أغنية

• `بحث` [اسم الأغنية]
  للبحث عن أغنية

**⚙️ الإعدادات:**
━━━━━━━━━━━━━━
• `الجودة` [عالية/متوسطة/منخفضة]
  لتغيير جودة التشغيل

• `حجم` [1-200]
  لتغيير مستوى الصوت

━━━━━━━━━━━━━━
💡 **نصيحة:** يمكنك إرسال رابط مباشر من يوتيوب أو سبوتيفاي!

👨‍💻 **المطور:** {developer}
📢 **القناة:** {channel}
"""
    
    ABOUT_TEXT = """
ℹ️ **معلومات عن البوت**

**📌 الاسم:** Seno Music Bot
**🤖 الإصدار:** 3.0 Advanced
**👨‍💻 المطور:** {developer}
**📢 القناة:** {channel}

**🎯 عن البوت:**
بوت احترافي لتشغيل الموسيقى في مكالمات تيليجرام الجماعية والقنوات، مع إمكانية التنزيل والبحث وقوائم التشغيل الذكية.

**💎 المميزات الخاصة:**
• تشغيل بجودة عالية HD
• دعم يوتيوب وسبوتيفاي
• تنزيل سريع وآمن
• واجهة عربية 100%
• حساب مساعد ذكي
• تحديثات دورية

**📊 الإحصائيات:**
• عدد المستخدمين: {users}
• عدد المجموعات: {groups}
• الأغاني المشغلة: {songs}

━━━━━━━━━━━━━━
🌟 **تم التطوير بـ ❤️ بواسطة سينو**
"""
    
    # رسائل الأخطاء
    ERROR_MSGS = {
        "no_permission": "❌ عذراً، ليس لديك صلاحية لاستخدام هذا الأمر!",
        "not_in_call": "❌ البوت ليس في مكالمة صوتية حالياً!",
        "already_playing": "⚠️ يوجد أغنية قيد التشغيل بالفعل!",
        "queue_empty": "📭 قائمة التشغيل فارغة!",
        "invalid_url": "❌ الرابط غير صالح! أرسل رابط يوتيوب صحيح.",
        "download_failed": "❌ فشل التنزيل! حاول مرة أخرى.",
        "not_admin": "❌ هذا الأمر متاح للمشرفين فقط!",
        "join_channel": "❌ يجب عليك الاشتراك في القناة أولاً!\n\n📢 اشترك الآن: {channel}",
        "search_failed": "❌ لم يتم العثور على نتائج!",
        "bot_not_admin": "❌ البوت يحتاج صلاحيات مشرف في المجموعة!",
    }
    
    # إعدادات الجودة
    QUALITY_SETTINGS = {
        "عالية": {"bitrate": 320, "resolution": "720p"},
        "متوسطة": {"bitrate": 192, "resolution": "480p"},
        "منخفضة": {"bitrate": 128, "resolution": "360p"}
    }
    
    # الحد الأقصى لقائمة التشغيل
    MAX_QUEUE_SIZE = 50
    
    # الحد الأقصى لمدة الأغنية (بالدقائق)
    MAX_SONG_DURATION = 30
    
    # الحد الأقصى لحجم التنزيل (بالميجابايت)
    MAX_DOWNLOAD_SIZE = 100
    
    # أوامر البوت
    COMMANDS = {
        "start": ["start", "بداية"],
        "help": ["help", "مساعدة"],
        "play": ["play", "شغل"],
        "pause": ["pause", "وقف"],
        "resume": ["resume", "استمرار"],
        "stop": ["stop", "ايقاف"],
        "skip": ["skip", "تخطي"],
        "queue": ["queue", "قائمة"],
        "download": ["download", "نزل"],
        "search": ["search", "بحث"],
        "about": ["about", "حول"],
    }
    
    @staticmethod
    def check_config():
        """التحقق من صحة الإعدادات"""
        errors = []
        
        if not Config.API_ID or Config.API_ID == 0:
            errors.append("❌ API_ID غير موجود!")
        
        if not Config.API_HASH:
            errors.append("❌ API_HASH غير موجود!")
        
        if not Config.BOT_TOKEN:
            errors.append("❌ BOT_TOKEN غير موجود!")
        
        if not Config.STRING_SESSION:
            errors.append("❌ STRING_SESSION غير موجود!")
        
        if not Config.OWNER_ID or Config.OWNER_ID == 0:
            errors.append("❌ OWNER_ID غير موجود!")
        
        if errors:
            print("\n".join(errors))
            print("\n⚠️ يرجى إكمال ملف .env بالمعلومات المطلوبة!")
            return False
        
        return True


# معلومات إضافية
BOT_INFO = {
    "name": "Seno Music Bot",
    "version": "3.0",
    "developer": "سينو (Seno)",
    "developer_username": "@idseno",
    "channel": "@senovip",
    "description": "بوت احترافي لتشغيل الموسيقى في تيليجرام",
}
